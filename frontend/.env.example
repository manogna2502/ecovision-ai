# Local development points to your local backend by default (see src/lib/api.js).
# For deployment (Vercel), set this as an environment variable in the dashboard —
# do not commit a real .env file with production values.
VITE_API_URL=https://ecovision-ai-idea2imapct.onrender.com
